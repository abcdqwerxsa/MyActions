# 批量同步镜像列表
#
# 用法：Actions → Sync Docker Image → mode 选 batch → 选 target_registry → Run
# 目标仓库整表共用一个：由触发时的 target_registry 决定，本文件不写仓库。
#
# 每个条目支持两种写法：
#   1) 纯字符串：只给镜像引用，tag 跟随源、目标名取最后一段
#   2) 对象：source 必填；可选字段：
#        target  覆盖目标镜像名（默认取 source 最后一段，自动去掉 library: 前缀）
#        tag     覆盖目标标签 / retag（默认跟随源 tag）
#                例如 source: nginx:1.25.3 + tag: stable → 拉 1.25.3，推 stable
#
# 源镜像须为公开可拉取（沿用 --src-no-creds）。单条失败不会中断整批。

images:
  - source: busybox:latest
  - source: langgenius/dify-api:1.17.0
  - source: langgenius/dify-web:1.17.0
  - source: postgres:15-alpine
  - source: mysql:8.0
  - source: redis:6-alpine
  - source: langgenius/dify-sandbox:0.2.15
  - source: langgenius/dify-agent-local-sandbox:1.17.0
  - source: langgenius/dify-plugin-daemon:0.6.10-local
  - source: langgenius/dify-agent-backend:1.17.0
  - source: ubuntu/squid:latest
  - source: certbot/certbot
  - source: nginx:latest
  - source: semitechnologies/weaviate:1.27.0
  - source: oceanbase/oceanbase-ce:4.3.5-lts
  - source: oceanbase/seekdb:latest
  - source: langgenius/qdrant:v1.8.3
  - source: pgvector/pgvector:pg16
  - source: vastdata/vastbase-vector
  - source: tensorchord/pgvecto-rs:pg16-v0.3.0
  - source: ghcr.io/chroma-core/chroma:0.5.20
  - source: containers.intersystems.com/intersystems/iris-community:2025.3
  - source: container-registry.oracle.com/database/free:latest
  - source: quay.io/coreos/etcd:v3.5.5
  - source: minio/minio:RELEASE.2023-03-20T20-16-18Z
  - source: milvusdb/milvus:v2.6.3
  - source: opensearchproject/opensearch:latest
  - source: opensearchproject/opensearch-dashboards:latest
  - source: opengauss/opengauss:7.0.0-RC1
  - source: myscale/myscaledb:1.6.4
  - source: matrixorigin/matrixone:2.1.1
  - source: docker.elastic.co/elasticsearch/elasticsearch:8.14.3
  - source: docker.elastic.co/kibana/kibana:8.14.3
  - source: downloads.unstructured.io/unstructured-io/unstructured-api:latest

  # 纯字符串写法（等价于只写 source）
  # - hello-world:latest
