# Image build configuration
# Modify this file to customize image name and tags

# Image name (owner prefix is auto-added, just write the image name)
image_name: cloud-browser-slim
# Tags (comma-separated, will be applied to the multi-arch manifest)
tags: latest,v1.3
